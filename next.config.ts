import type { NextConfig } from 'next';

const nextConfig: NextConfig = {
	/* config options here */
	images: {
		domains: ['i.ytimg.com'], // Add any other external hostnames if needed
	},
};

export default nextConfig;
